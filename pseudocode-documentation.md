Create a Person struct with name and number

Create a Queue class with:
    an array of Person
    frontIndex
    count

Set frontIndex to 0
Set count to 0

Manually place 5 people into the array
Set count to 5

While the queue is not empty:
    get the person at frontIndex
    print greeting with name and number
    move frontIndex forward by 1
    subtract 1 from count

Print closing message
